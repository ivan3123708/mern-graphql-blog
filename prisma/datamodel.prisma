type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  posts: [Post!]!
}

type Post {
  id: ID! @id
  title: String!
  text: String!
  author: User @relation(link: INLINE)
}